'use client'

import JournalAgentPanel from '../JournalAgentPanel'

// Routed home for the Journal Analysis agent panel (auth + nav from layout).
export default function JournalAgentPage() {
  return <JournalAgentPanel />
}
