'use client'

import ContentScheduler from '../ContentScheduler'

// Routed home for the content scheduler (auth + nav from layout).
export default function SchedulerPage() {
  return <ContentScheduler />
}
