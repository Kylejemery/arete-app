'use client'

import CorpusAgentPanel from '../CorpusAgentPanel'

// Routed home for the RAG Corpus agent panel (auth + nav come from the layout).
export default function CorpusAgentPage() {
  return <CorpusAgentPanel />
}
