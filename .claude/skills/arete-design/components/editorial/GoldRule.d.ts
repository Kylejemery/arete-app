export interface GoldRuleProps {
  /** 64px under academy headings, 40px (at 2px tall) on the share card. */
  width?: number;
  height?: number;
  align?: 'left' | 'center';
  style?: React.CSSProperties;
}
export declare function GoldRule(props: GoldRuleProps): JSX.Element;
