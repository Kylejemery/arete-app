export interface StreakPillProps {
  /** Current streak, shown beside a filled flame. */
  count: number | string;
  style?: React.CSSProperties;
}
export declare function StreakPill(props: StreakPillProps): JSX.Element;
