One-line: the base flat surface everything in Arete sits on — borders, never shadows.

```jsx
<Card border="hairline" accentRule padding={20}>…</Card>
<Card done>Cold shower</Card>
```

Raise `border` to `selected` for a chosen card. Never add a box-shadow; never use a lighter fill for emphasis.
