One-line: a subscription tier on the paywall — this component uses the academy navy set, not the app surfaces, because the paywall is deliberately more editorial.

```jsx
<PlanCard badge="Best value" name="Annual" note="Two months free" price="$59.99" period="per year" selected />
```
