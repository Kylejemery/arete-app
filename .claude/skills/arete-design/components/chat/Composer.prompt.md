One-line: the message bar at the bottom of the Cabinet — pill field on ink, 40px gold send key.

```jsx
<Composer value={text} onChange={e => setText(e.target.value)} onSend={send} disabled={!text} />
```
