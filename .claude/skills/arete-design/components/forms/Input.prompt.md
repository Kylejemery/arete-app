One-line: a text field — ink fill inside a surface card, gold hairline, 16px white text, #555 placeholder.

```jsx
<Input placeholder="What should we call you?" />
<Input multiline placeholder="On patience with slow progress" />
```

A card being edited gets a solid gold border around it (see EditCard), not a border change on the field.
