One-line: the in-place editor a dashed "add" affordance turns into — solid gold border, underline field, ghost Cancel beside a small primary.

```jsx
<EditCard placeholder="Walk before checking the phone" onCancel={close} onConfirm={save} />
```
