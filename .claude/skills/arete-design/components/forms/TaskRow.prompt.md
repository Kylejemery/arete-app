One-line: a routine item — the app's checkbox, where "done" inverts the entire card to gold.

```jsx
<TaskRow title="Cold shower" done />
<TaskRow title="Read 20 pages of Meditations" note="Book II" onToggle={toggle} />
```

The inversion is the whole point; don't reduce it to a gold checkmark on a dark card.
