One-line: the top of a screen — gold 26px title, optional subtitle and small-caps roster, one trailing control.

```jsx
<ScreenHeader title="The Cabinet" subtitle="Your counselors, in session"
  smallCaps="Marcus · Seneca · Epictetus · Future Self" divider
  right={<IconButton variant="header"><Icon name="add" /></IconButton>} />
```
