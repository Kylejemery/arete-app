One-line: the home screen's morning/evening pair of fully-rounded pills.

```jsx
<RoutineToggle value="evening" onChange={setRoutine} />
```

Legacy: these are the only emoji left in the UI. New screens should pass `useEmoji={false}` and place an `<Icon>` instead.
