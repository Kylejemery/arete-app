One-line: an in-content switch — a surface track with 4px padding whose active segment is a solid gold block.

```jsx
<SegmentedTabs items={['Read','Books','History']} value={tab} onChange={setTab} />
```

Use this inside a screen. For top-of-screen navigation use UnderlineTabs instead.
