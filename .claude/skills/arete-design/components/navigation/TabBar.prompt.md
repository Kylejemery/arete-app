One-line: the app's 60px bottom tab bar — ink, no top border, gold active tint, grey inactive.

```jsx
<TabBar value="home" onChange={setTab} />
```

Eight tabs ship: Home, Morning, Evening, Cabinet, Journal, Focus, Scrolls, Progress. Labels are 9px.
