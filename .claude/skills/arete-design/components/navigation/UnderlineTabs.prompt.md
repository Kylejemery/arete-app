One-line: top-of-screen tabs — surface strip, 13% gold divider, 2px gold underline on the active tab.

```jsx
<UnderlineTabs items={['Cabinet','Counselors','Sessions']} value={tab} onChange={setTab} />
```
