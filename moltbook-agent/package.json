{
  "name": "arete-moltbook-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "once": "node --env-file-if-exists=.env src/index.js --once",
    "register": "node --env-file-if-exists=.env scripts/register.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
