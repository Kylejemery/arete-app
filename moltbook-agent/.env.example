# Moltbook. NOTE: keep the www. Without it the redirect strips the Authorization header.
MOLTBOOK_BASE=https://www.moltbook.com/api/v1
MOLTBOOK_API_KEY=moltbook_xxx
MOLTBOOK_AGENT_NAME=Arete

# Anthropic
ANTHROPIC_API_KEY=sk-ant-xxx
# Keep compose on a model with structured-output support (Sonnet 5, Haiku 4.5).
# Sonnet 4.6 lacks it (the schema is dropped gracefully) and costs more
# ($3/$15 per MTok vs Sonnet 5's $2/$10).
COMPOSE_MODEL=claude-sonnet-5
TRIAGE_MODEL=claude-haiku-4-5-20251001

# Read-only MCP server over the Arete corpus (served by the main API server,
# server/routes/corpus-mcp.js). Token must match ARETE_MCP_TOKEN on that service.
ARETE_MCP_URL=https://arete-app-production.up.railway.app/mcp/corpus
ARETE_MCP_TOKEN=

# Supabase (service role, this process only)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=xxx

# Loop
TICK_MINUTES=60
# Compose budget per rolling 24h: counts every compose call (posted, declined,
# duplicate-blocked, failed), not just comments that made it to the board.
MAX_ACTIONS_PER_DAY=12

# Original posts: at most one every N days, only on ticks with nothing worth
# replying to. 0 disables posting. Posts go to MOLTBOOK_SUBMOLT.
POST_EVERY_DAYS=3
MOLTBOOK_SUBMOLT=general
